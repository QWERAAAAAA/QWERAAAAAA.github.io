执行测试数据是没错的，格式也没错，但是还有极端数据不知道怎么测啊

主要还是print函数的实现问题。

```C++
#include<iostream>
#include<iomanip>
#include<cctype>
using namespace std;

class Vehicle{
    protected:
        int wheels, weight;
    public:
        Vehicle(int n_wheels, int n_weight){
            wheels = n_wheels;
            weight = n_weight;
        }
        int get_wheels(){
            return wheels;
        }
        int get_weight(){
            return weight;
        }
        float wheel_load(){
            return (float)weight/wheels;
        }
        void print(int count,string sort,int countV){
            string name = 'V' + sort.substr(1);
            cout << fixed << setprecision(2);
            if(count==1)
                cout<<"The 1st object is "<<name<<" No. "<<countV<<": weight "<<weight<<" Kg and wheels "<<wheels<<endl;
            else if(count == 2)
                cout<<"The 2nd object is "<<name<<" No. "<<countV<<": weight "<<weight<<" Kg and wheels "<<wheels<<endl;
            else if(count == 3)
                cout<<"The 3rd object is "<<name<<" No. "<<countV<<": weight "<<weight<<" Kg and wheels "<<wheels<<endl;
            else
                cout<<"The "<<count<<"th object is "<<name<<" No. "<<countV<<": weight "<<weight<<" Kg and wheels "<<wheels<<endl;
        }
};

class Car: public Vehicle{
    int passenger_load;
    public:
        Car(int n_wheels, int n_weight, int n_passenger):Vehicle(n_wheels,n_weight){
            passenger_load = n_passenger;
        }
        int get_passengers(){
            return passenger_load;
        }
        void print(int count, string sort, int countC){
            string name = 'C' + sort.substr(1);     // 实现首字母大写
            cout << fixed << setprecision(2);
            if(count==1)
                cout<<"The 1st object is "<<name<<" No. "<<countC<<": passenger_load "<<passenger_load<<" weight "<<weight<<" Kg and wheels "<<wheels<<endl;
            else if(count == 2)
                cout<<"The 2nd object is "<<name<<" No. "<<countC<<": passenger_load "<<passenger_load<<" weight "<<weight<<" Kg and wheels "<<wheels<<endl;
            else if(count == 3)
                cout<<"The 3rd object is "<<name<<" No. "<<countC<<": passenger_load "<<passenger_load<<" weight "<<weight<<" Kg and wheels "<<wheels<<endl;
            else
                cout<<"The "<<count<<"th object is "<<name<<" No. "<<countC<<": passenger_load "<<passenger_load<<" weight "<<weight<<" Kg and wheels "<<wheels<<endl;
        }
};

class Truck: public Vehicle{
    int passenger_load, payload;
    public:
        Truck(int n_wheels, int n_weight, int n_passenger,int load):Vehicle(n_wheels,n_weight){
            passenger_load = n_passenger;
            payload = load;
        }
        int get_passengers(){
            return passenger_load;
        }
        float efficiency(){
            return (float)payload/(payload+weight);
        }
        void print(int count,string sort,int countT){
            string name = 'T' + sort.substr(1);
            cout << fixed << setprecision(2);
            if(count==1)
                cout<<"The 1st object is "<<name<<" No. "<<countT<<": passenger_load "<<passenger_load<<" weight "<<weight<<" Kg and wheels "<<wheels<<" and efficiency "<< efficiency() <<endl;
            else if(count == 2)
                cout<<"The 2nd object is "<<name<<" No. "<<countT<<": passenger_load "<<passenger_load<<" weight "<<weight<<" Kg and wheels "<<wheels<<" and efficiency "<< efficiency() <<endl;
            else if(count == 3)
                cout<<"The 3rd object is "<<name<<" No. "<<countT<<": passenger_load "<<passenger_load<<" weight "<<weight<<" Kg and wheels "<<wheels<<" and efficiency "<< efficiency() <<endl;
            else
                cout<<"The "<<count<<"th object is "<<name<<" No. "<<countT<<": passenger_load "<<passenger_load<<" weight "<<weight<<" Kg and wheels "<<wheels<<" and efficiency "<< efficiency() <<endl;
        }
};


int main(){
    string sort;
    int count = 0;          // 记录是第几个数据
    while (true)
    {
        cin>>sort;
        if(sort[0] == '-')
            break;
        if(sort[0] == 'v'){
            int m_wheel,m_weight,num;
            count += 1;
            cin>>num>>m_wheel>>m_weight;
            Vehicle obj(m_wheel,m_weight);
            obj.print(count,sort,num);

        }
        if(sort[0] == 'c'){
            int m_wheel,m_weight,people,num;
            count += 1;
            cin>>num>>m_wheel>>m_weight>>people;
            Car obj(m_wheel,m_weight,people);
            obj.print(count,sort,num);
        }
        if(sort[0] == 't'){
            int m_wheel,m_weight,people,load,num;
            count += 1;
            cin>>num>>m_wheel>>m_weight>>people>>load;
            Truck obj(m_wheel,m_weight,people,load);
            obj.print(count,sort,num);
        }
    }
    return 0;
    
}
```

全部测试点都AC的例子，让我研究研究差在哪了

```C++
#include<iostream>  
#include<iomanip>  
using namespace std;  
 
class vehicle  
{  
protected:  
	int wheels;  
	float weight;  
	
public:  
	vehicle(float wh,float we):wheels(wh),weight(we){};  
	int get_wheels(){return wheels;}  
	float get_weight(){return weight;}  
	float wheel_load(){return weight/wheels;}  
	void print()
	{  
		cout<<"weight "<<weight<<" Kg and wheels "<<wheels<<endl;  
	}
};  
 
class car:protected vehicle  
{ 
private: 
	int passenger_load;  
	
public:  
	car(float wh,float we,int pa):vehicle(wh,we),passenger_load(pa){};  
	int get_passenger(){return passenger_load;}  
	void print()
	{  
		cout<<"passenger_load "<<passenger_load<<" weight "
		<<weight<<" Kg and wheels "<<wheels<<endl;  
	}	 
};  
 
class truck:protected vehicle  
{
private:
    int passenger_load;  
    int payload;  
public:  
    truck(float wh,float we,int pa,int load):vehicle(wh,we),passenger_load(pa),payload(load){};  
    int get_passengers(){return passenger_load;}  
    float efficiency(){return payload/(payload+weight);}  
    void print()
	{  
		cout<<"passenger_load "<<passenger_load<<" weight "<<weight
		<<" Kg wheels "<<wheels<<" and efficiency "<<setiosflags(ios::fixed)
		<<setprecision(2)<<efficiency()<<setprecision(0)<<endl;  
	}
};  
  
int main()  
{  
    string type;  
    int ID,wheel,weight,passenger_load,payload,i=1;  
    cin>>type;  
    while(type!="-1")  
    {  
		cin>>ID>>wheel>>weight;  
		cout<<"The "<<i<<"";//无奈之举 
		if(i==1)
			cout<<"st";  
		else if(i==2)  
			cout<<"nd";  
		else if(i==3)  
			cout<<"rd";  
		else  
			cout<<"th";  
		i++;  
		cout<<" object is ";  
		if(type=="vehicle")  
		{  
			cout<<"Vehicle"<<" No. "<<ID<<": ";  
			vehicle t(wheel,weight);  
			t.print();  
		}  
		if(type=="car")  
		{  
			cin>>passenger_load;  
			cout<<"Car"<<" No. "<<ID<<": ";  
			car t(wheel,weight,passenger_load);  
			t.print();  
		}  
		if(type=="truck")  
		{  
			cin>>passenger_load>>payload;  
			cout<<"Truck"<<" No. "<<ID<<": ";  
			truck t(wheel,weight,passenger_load,payload);  
			t.print();  
		}  
		
		cin>>type;  
    }  
    
	return 0;  
}  

```

